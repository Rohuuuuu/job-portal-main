{
  "name": "job-portal",
  "version": "1.0.0",
  "description": "Job Portal Application",
  "main": "index.js",
  "scripts": {
    "start": "cd backend && npm install && npm start",
    "dev": "cd backend && npm install && npm run start",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install"
  },
  "author": "",
  "license": "ISC"
}